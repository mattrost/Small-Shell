Matthew Rost
REAMDE for Program 3

To run this file, put both Makefile and smallsh.c in a directory (dir) with the files to be tested:

cd dir
make

If you would like to use the program, use:
./smallsh

To run a test script, place the test script p3testscript in the same directoy as the compiled shell and run (chmod +x ./p3testscript)
Then run the ccommand using "./p3testscript 2>&1"